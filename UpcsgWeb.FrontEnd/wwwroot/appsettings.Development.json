{
  "//": "Local development only. Blazor loads appsettings.json first, then this file overrides it when the dev server reports the Development environment. Production (GitHub Pages) never sees this, so the Render URL belongs in appsettings.json instead.",
  "Api": {
    "BaseUrl": "http://localhost:5027"
  },

  "//Google": "The same client id as production. The API validates the token's audience against Google:ClientId, so a different id here is rejected as an invalid token rather than falling back to the stand-ins. Not a secret - every browser that loads the site downloads it.",
  "Google": {
    "ClientId": ""
  }
}
